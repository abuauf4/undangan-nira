'use client'

// This page renders the same invitation as the root page (/),
// but the URL contains a guest slug (e.g., /budi-santoso).
// CoverSection will pick up the slug via useParams() and fetch guest data.

export { default } from '../page'
